package com.example.demo.service;


import org.springframework.stereotype.Service;


@Service
public class AngleService {
    // private Angle angle;
    // private Double val = Double.parseDouble(angle.getValue());
    // private Double minval = val*0.95;
    // private Double maxval = val*1.05;

    // private Double correctValue = 90.0;

    //계산해서 맞는지틀렸는지 알려줘야함.

    // public String checkAngle(Angle angle) {
    //     Double val = Double.valueOf(angle.getValue());
    //     System.out.println(val);
    //     Double minval = val*0.95;
    //     Double maxval = val*1.05;
    //     //angle 위치에 따라 자세에 따라 correct 포즈 다를것임;;
    //     Double correctValue = 90.0;
    //     String returnVal="correct pose";
    //     returnVal = (maxval < correctValue) ? "Too straight" : "Correct Pose";
    //     returnVal = (minval > correctValue) ? "Too curved" : "Correct Pose";


    //     return returnVal;
    // }
    
}
